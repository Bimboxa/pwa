/*
* compute the props for one entity based on joins and formulas
*
* entity: entity object
* props: array of props to compute. ex: ['image.url', 'category','category.fullName','material.category.fullName']
*
* notes: 
- this function is recursived to handle nested properties.
*/

export default function resolveEntityPropsAsync({entity, props, listing}) {}
