export default function getEntityPropObject({
  entity,
  propKey,
  entityModel,
  listing,
}) {
  // resolve the entity object based on the entity model and listing
  const resolvedEntity = {};

  return resolvedEntity;
}
