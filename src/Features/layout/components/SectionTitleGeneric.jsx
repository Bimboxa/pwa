
import { Typography } from "@mui/material";

export default function SectionTitleGeneric({ title }) {
    return <Typography variant="body2" color="text.secondary" sx={{ p: 1 }}>{title}</Typography>
}
